package ottawa.ventilator;

public class CodeFromBrian {

    /*
        if one of you has an arduino, upload this script to it
        it will help you test serial
        i can make it so it sends a serial command back too


        void setup() {
          Serial.begin(9600);
        }

        String readSerial(){
          char commandString[20]={};
          uint8_t i=0;
          while(Serial.available()>0){
            commandString[i] = Serial.read();
            i++;
          }
          return commandString;
        }

        void loop() {
          String command = readSerial();
          Serial.print(command);

        }

     */
}

